#include "fmath.h"

double _identity(const double a, const double b) { return a; }

double _add(const double a, const double b) { return a + b; }

double _mult(const double a, const double b) { return a * b; }

double _pow(const double a, const double b) { return pow(a,b); }   
